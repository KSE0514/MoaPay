spring:
  application:
    name: member

  config:
    import: "configserver:http://localhost:8888"

  cloud:
    config:
      name: member
      profile: local

server:
  servlet:
    session:
      timeout: 30m  # 세션 타임아웃을 30분으로 설정

